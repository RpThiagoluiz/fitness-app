import { NavigationContainer } from '@react-navigation/native';
import { TabsRoutes } from './TabsRoutes';

export const Routes = () => {
  return (
    <NavigationContainer>
      <TabsRoutes />
    </NavigationContainer>
  );
};
